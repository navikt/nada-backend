mutation {
  removeFromCollection (
    id: "{{ .collection_id }}"
    elementID: "{{ .dataproduct_id }}"
    elementType: dataproduct
  )
}

RETURNS

{
  "data": {
    "removeFromCollection": true
  }
}
