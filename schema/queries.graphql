scalar Time

directive @goField(
    forceResolver: Boolean
    name: String
) on INPUT_FIELD_DEFINITION | FIELD_DEFINITION

type Query {
	version: String!
}

type Mutation {
	dummy(no: String): String
}
